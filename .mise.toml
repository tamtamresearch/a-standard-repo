# Tool version management for standard-repo POC

[tools]
# Node.js for ReSpec and build scripts
node = "22"

# Python for deployment scripts
python = "3.12"

# Note: PATH modification moved to shell activation instead of .mise.toml
# mise doesn't support PATH arrays - use shell rc files if needed
